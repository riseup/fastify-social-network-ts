{
  "name": "fastify-social-network-ts",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/jest": "^29.5.12",
    "@types/node": "^22.5.0",
    "dotenv": "^16.4.5",
    "jest": "^29.7.0",
    "rimraf": "^6.0.1",
    "ts-jest": "^29.2.4",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.5.4"
  },
  "scripts": {
    "start": "node dist/index.js",
    "build": "npm run clean && tsc",
    "clean": "rimraf dist",
    "dev": "ts-node-dev src/index.ts --respawn",
    "test": "jest",
    "db:up": "docker-compose --env-file .env -f docker/docker-compose.yml up -d",
    "db:down": "docker-compose -f docker/docker-compose.yml down && docker volume rm docker_postgres_data"

  },
  "engines": {
    "node": ">=20.12.2"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "pg": "^8.12.0",
    "pino": "^9.3.2",
    "reflect-metadata": "^0.2.2",
    "typeorm": "^0.3.20",
    "zod": "^3.23.8"
  }
}
